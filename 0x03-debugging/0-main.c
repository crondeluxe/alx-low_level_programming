#include "main.h"

/* function declaration */
void positive_or_negative(int i);

/**
 * main - debugging the code
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	positive_or_negative(0);
	return (0);
}
